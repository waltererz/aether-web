import reduxConstants from '../constants';

const initialStates = {
    
};

function app(state = initialStates, action) {
    switch (action.type) {
        
    }

    return state;
}

export default app;
